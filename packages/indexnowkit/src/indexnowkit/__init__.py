"""IndexNow for Python: the core of the indexnowkit family.

The public surface of 0.1.0 (spec 20 §3.1) arrives module by module in wave P step 1; until then the package
exposes its version only. ``__version__`` is the single source of the distribution version (hatch reads it).
"""

__version__ = "0.1.0.dev0"

__all__ = ["__version__"]
